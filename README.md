# Student_Management_Operation_JDBC
CRUD operations in JDBC are used to perform database operations from Java applications. Create inserts records, Read retrieves records, Update modifies existing records, and Delete removes records. JDBC uses Connection, PreparedStatement, ResultSet, and execute methods to perform these operations.
